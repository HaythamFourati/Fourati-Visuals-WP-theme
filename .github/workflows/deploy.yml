name: Publish Website

on:
  push:
    branches:
      - main

jobs:
  web-deploy:
    name: 🚀 Deploy Website Every Commit
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get Latest Code
        uses: actions/checkout@v3

      - name: 📂 Sync Files
        uses: SamKirkland/web-deploy@v1
        with:
          target-server: fourativisuals.com
          remote-user: u535044671
          private-ssh-key: ${{ secrets.SSH_KEY }}
          destination-path: /home/u535044671/fourativisuals.com/wp-content/themes/Fourati-Visuals-Custom-Theme/
